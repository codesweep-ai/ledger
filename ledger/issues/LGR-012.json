{
  "id": "LGR-012",
  "title": "The fixture oracle freezes this repo's live ledger, so adding a record fails it",
  "type": "defect",
  "severity": "med",
  "status": "closed",
  "foundBy": "session agent, re-running viewer/fixtures after the @codesweep-ai/ui port",
  "opened": "2026-09-01",
  "resolved": "2026-09-22",
  "stint": "pilot2-tier1",
  "evidence": {
    "commits": [
      "40b30b9",
      "c9945b8"
    ],
    "integrated": [],
    "verified": "`make fixtures` with Chrome 153.0.8010.52. At a48d212 it failed 7 rows, LF-02, 03, 04, 12, 13, 14 and 15, and the full `--json` values moved only by LGR-013 and LGR-014. At 40b30b9 and at c9945b8 it returned PASS 29, DONE 18, FAIL 0.\n\nA throwaway record was added to a copy of `ledger/` and never committed: LGR-015, high severity, in progress. Against that copy the runner at c9945b8 passed all 29 keep rows. The runner at 40b30b9 failed 9: LF-02, 03, 04, 09, 12, 13, 14, 15 and 16.\n\n`make fixtures-model` passes 4 of 4 tests. With the board's closed lane modelled as expanded, its twin test fails, and the runner fails LF-02 naming LF-06. `make ci` at c9945b8 ran every gate and exited 0."
  },
  "resolution": "The own-ledger rows now compute the counts, ids, lanes, groups, orders and first card from the records the page is rendered from. That is the second fix the details proposed. The model doing it, `viewer/fixtures/derive.mjs`, must reproduce each row's frozen sandbox twin on every run, and `make fixtures-model` checks the same in CI. Filing a record or starting work on one no longer spends a reviewer's approval.",
  "details": "`viewer/fixtures` runs every behavioural check against two corpora: this repository's own `ledger/`, and the frozen `fixtures/sandbox/ledger`. The `own` half records literal record ids and counts as frozen `keep` values. `ledger/` is the one corpus that grows. So a check that measures the viewer fails whenever the ledger it is pointed at changes.\n\nAdding LGR-011 was enough. Seven checks failed, and none of them describes a viewer defect:\n\n| check | what it froze |\n|---|---|\n| LF-02, LF-03, LF-04 | visible record set on own/board, own/list, own/activity |\n| LF-12, LF-13 | the count behind `status - open` and `sev - crit` |\n| LF-14 | a five-id sort order, `[LGR-001, LGR-009, LGR-010, LGR-008, LGR-007]` |\n| LF-15 | the count `reset` returns to |\n\nEvery one has an exact twin against the sandbox corpus. Those twins are LF-06, LF-07, LF-08, LF-20, LF-21, LF-22 and LF-23, and all seven passed. The failures therefore left no behaviour unasserted.\n\nClosing a record trips it as readily as adding one. LF-12 freezes the open and closed split, and LF-01 freezes the top of the queue as `[LGR-009, LGR-010]`. LF-01 passed only because LGR-011 did not surface in the brief view. About thirteen `own` checks are literal in this way, so six more are latent.\n\nThe cost is not the churn. Re-recording a `keep` row is gated on an approval id and a reason. `viewer/fixtures/README.md` reserves that for the case where a reviewer has agreed a behaviour genuinely changed. Spending the sign-off on a ledger that gained a record is how it becomes a rubber stamp. That disarms it for the change it exists to catch.\n\nNone of this is urgent. `make fixtures` sits outside `make check` and `make ci`, so nothing is red. The live-corpus value of `own` also survives, because its non-literal checks do not drift. LF-44 measures zero page errors and no external requests, and the axe, keyboard and size rows compare against thresholds.\n\nThere are two reasonable fixes. The first drops the literal-equality `own` variants that already have a frozen twin. That costs no behavioural coverage, but it gives up a second dataset shape, because sandbox renders three board lanes to own's two. Freezing a snapshot of `own` as a third corpus recovers it, which is what the sandbox corpus already is. The second makes the `own` variants relative, asserting invariants derived from the payload rather than frozen literals. That keeps the symmetry, at the cost of surgery on `run.mjs`.",
  "notes": [
    {
      "date": "2026-09-01",
      "text": "The latent set is not theoretical. LF-11, a search over `LGR-00`, drifted as soon as LGR-012 landed, so the approved re-record covered eight rows rather than seven. Appending this note moves the activity count in turn, which is the fault working as described."
    },
    {
      "date": "2026-09-22",
      "text": "The second pilot of the deps-maintenance campaign showed what the details called not urgent. `make fixtures` had failed seven rows at every commit since LGR-013 was filed on 2026-09-09. Both pilots gate on this oracle even though `make ci` does not, so each carried a red-at-base clause through every verification rule."
    }
  ],
  "links": []
}
