Vendored from the @codesweep-ai/ui design system (read-only upstream)
package: @codesweep-ai/ui v1.12.0 (released 2026-08-07, tag v1.12.0)
files: src/styles/tokens.css, src/styles/base.css
copied: 2026-08-06; version metadata synced 2026-08-07 on release

Two-copy structure (deliberate, do not deduplicate): this directory is the
PRISTINE upstream mirror, byte-for-byte including the @tailwind directives.
The SHIPPED copies live at viewer/tokens.css + viewer/base.css — adapted for
the no-build rendered page and embedded into the cs-ledger binary at build.
The adaptation touches four things and nothing else: the @tailwind directives
are stripped, a provenance header is added, two comments lose the
"(unreleased)" note this mirror was copied with, and two comment lines in
base.css trade backticks for quotes. Run `diff -u vendor/codesweep-ui/base.css
viewer/base.css` (and the same for tokens.css) to see the whole of it.
Re-vendoring: refresh this mirror from upstream, re-apply the adaptation to
viewer/, make build.
